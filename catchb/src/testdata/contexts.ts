export const defaultAuthContext = {
  login: jest.fn(),
  socialLogin: jest.fn(),
  logout: jest.fn(),
  mode: "guest",
};

export const defaultThemeContext = {
  theme: {},
  colorScheme: "light",
};
